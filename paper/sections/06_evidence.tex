\section{Evidence from the ESS and the AmericasBarometer}\label{sec:evidence}

\paragraph{The regime characterization.} On real data the honest band reduces to
clustered conformal---instructively. Across both surveys, both estimands, and
three outcomes, cross-national inference is a uniformly \emph{low design-noise}
regime:
$\rhohat\le0.22$ on LAPOP, at most $0.29$ in the ESS
subgroup scan, well below the $\rho_0=0.47$ cutoff. Two directional caveats attach
to every $\rhohat$ we report: the PSU bootstrap resamples $m$-of-$m$ without
Rao--Wu--Yue rescaling \citep{rao1992recent} and single-PSU strata contribute no
variance, so the estimates are downward-biased \emph{floors}. The Rao--Wu--Yue rescaled rerun confirms the floors are tight: maxima $0.281$
(ESS) and $0.222$ (LAPOP), still severalfold below $\rho_0$, zero gate
activations, every certification count unchanged; and singleton strata, which no
rescaling repairs, are $1$ of $7{,}028$ in the core rounds. The safe selector
thus reduces to clustered PCB on all real data---not by assumption but by its own
low-$\rho$ gate---at the national unit, which the frontier below qualifies.

\paragraph{Why the deconvolution is unreachable, precisely.}

\begin{proposition}[Survey-scale unreachability]\label{prop:unreach}
The deployed selector activates deconvolution only if a \emph{need} gate
$\rhohat_{\mathrm{LCB}}>\rho_0$ and a \emph{reliability} gate $D\le\tau_D$ both
hold, $D=\max_t \widehat{\mathrm{SE}}(\widehat s_T^2)/\widehat s_T^2$. Both are
bounded by \emph{algorithmic identities of the frozen procedure}.
\emph{(a)} $\rho=\sqrt{\overline{v^2}}/s_{\mathrm{plug}}\in[0,1)$ with
$s_{\mathrm{plug}}^2=s_R^2+\overline{v^2}$: the need gate fires only when design
noise is an appreciable fraction of the between-population signal.
\emph{(b)} Since $\widehat s_T\le s_{\mathrm{plug}}$ by construction,
$D\ge\sqrt{2/(K-1)}$, so the reliability gate requires $K\ge1+2/\tau_D^2$, i.e.\
$K\ge94$ at the frozen $\tau_D=0.147$. (Appendix~\ref{app:proofs} separates the
deterministic statement from its kurtosis-dependent interpretation.)
\end{proposition}

The two requirements pull apart on real surveys (Figure~\ref{fig:unreach}). On
the ESS the noisiest subpopulations raise $\rhohat$ only to $0.29$ while $K\le33$
leaves $D\ge0.25$: \emph{neither} gate opens ($42$ cells, zero activations). The
WVS is the mirror image---its full item-coverage sets ($K=95$--$105$) make the
reliability gate feasible, but the certification samples ($K=59$--$77$) sit below
$94$, and in either denominator the weights-only
$\rhohat_{\mathrm{LCB}}\le0.09$ fails the need gate decisively. The survey large
enough to estimate the deconvolution has almost no design noise to remove; the
survey with real clustered noise has far too few countries.

\paragraph{The barrier belongs to the unit, not to surveys.} Both gates are
statements about how many exchangeable populations a survey contains and how
much sample sits behind each, so refining the unit moves them together: $K$
grows, relieving the floor $D\ge\sqrt{2/(K-1)}$, while the sample behind each
population shrinks, raising $\rho$. ESS regions, with PSUs nested inside so each
carries a genuine stratified-PSU bootstrap, let us trace that frontier on real
survey data (\texttt{e49}, Figure~\ref{fig:frontier}); the estimand there is the
small-area one this regime concerns, a region's departure from its own national
distribution. Sweeping the minimum region size from $400$ respondents to $25$
takes $K$ from $21$--$30$ to $300$--$315$ and $\rhohat_{\mathrm{LCB}}$ from
$0.08$ to $0.47$. The reliability gate opens in every round below $80$
respondents per region, and at $25$--$30$ the need gate opens too, in one of
three rounds ($\rhohat_{\mathrm{LCB}}=0.476$ against $\rho_0=0.47$): \emph{both
barriers of Proposition~\ref{prop:unreach} are reachable}, two orders of
magnitude below the country. The selector still abstains, for a third reason---the
deconvolution on offer is $13.9\%$ narrower and the frozen efficiency gate asks
for $10\%$ after a five-point margin. At the national unit the correction is
unavailable; at the subnational unit it is available and not yet worth taking.

\paragraph{How coarse should the unit be? A feasibility floor, not an optimum.}
The same refinement that opens the gates also sets the band's width, and the
direction is not obvious a priori: refining multiplies $K$, which tightens the
conformal quantile, but shrinks the sample behind each unit, which inflates the
design noise the band must carry. We tested it with the confounds removed (\texttt{e52}): within each
country-round we build units of a \emph{target} size by grouping whole PSUs at
random, so size is controlled rather than filtered, composition is fixed, and
each unit keeps its own stratified-PSU bootstrap. Over the range where the
country set is constant (targets of $40$ to $350$ respondents), the half-width
falls \emph{monotonically} as units coarsen, $0.190$ to $0.065$: there is no
interior optimum and refinement is strictly costly. An earlier sweep over NUTS
regions appeared to show one; the Supplementary Material reports why it did not
survive, because the artifact is easy to reproduce. What does bind is a
corner. The conformal level is \emph{infeasible} whenever
$K<\alpha^{-1}-1$: the required order statistic exceeds the sample and the band
is $[0,1]$ at any width. The practical rule is therefore the opposite of
refinement---take the coarsest unit your question admits, subject to
$K\ge\alpha^{-1}-1$---and the cross-national default is on the right side of it
($K=30$--$34$ against a floor of nine). This is a sharp and, as far as we know,
unremarked constraint on cluster-conformal practice; it is a statement about the
\emph{calibration} unit of a transport band, and it does not bear on the unit of
an estimand, which a research question fixes. Unit choice in the substantive
sense---which areal partition to analyze---is the modifiable areal unit problem
\citep{openshaw1984modifiable,robinson1950ecological}, and we are not adding to
it.

\begin{figure}[t]\centering
\includegraphics[width=\textwidth]{unit_frontier.pdf}
\caption{Refining the unit relieves both barriers (ESS regions, rounds 9--11;
medians over rounds, shaded range). \emph{Left:} the remainder bound falls past
$\delta_{\max}$, in every round by $80$ respondents per region. \emph{Right:}
$\rhohat_{\mathrm{LCB}}$ reaches $\rho_0$, crossing in one round at
$25$--$30$.}\label{fig:frontier}
\end{figure}

\begin{figure}[t]\centering
\includegraphics[width=\textwidth]{unreachability.pdf}
\caption{The two barriers at the national unit. \emph{Left:} ESS age-band
subpopulations ($42$ cells), $\rhohat$ vs $\rho_0$. \emph{Right:} $D$ vs its floor
$\sqrt{2/(K-1)}$; only WVS full item-coverage sets reach $K\ge94$, and there the
need gate fails.}\label{fig:unreach}
\end{figure}
